# Scoop bucket for the BurnedSecret CLI

    scoop bucket add burnedsecret https://github.com/JensrudJ/scoop-burnedsecret
    scoop install bs

Manifests are rendered and pushed by CI on each `cli-v*` release. Install docs: https://burnedsecret.com/install/cli/
